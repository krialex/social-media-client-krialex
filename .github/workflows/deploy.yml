name: CI/CD Workflow

on:
  push:
    branches: ['workflow']
  pull_request:
    branches: ['workflow']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm install
      - name: Run ESLint
        run: npm run lint
      - name: Run Prettier Check
        run: npm run format

  unit-tests:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm install
      - name: Run unit tests
        run: npm run test

  e2e-tests-critical:
    runs-on: ubuntu-latest
    needs: unit-tests
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm install
      - name: Start live server
        run: npm run start &
        env:
          PORT: 8080
      - name: Wait for server to be ready
        run: npx wait-on http://127.0.0.1:8080
      - name: Run critical e2e tests
        env:
          CYPRESS_BASE_URL: http://127.0.0.1:8080
        run: npm run test:e2e:critical

  e2e-tests-non-critical:
    runs-on: ubuntu-latest
    needs: e2e-tests-critical
    continue-on-error: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm install
      - name: Start live server
        run: npm run start &
        env:
          PORT: 8080
      - name: Wait for server to be ready
        run: npx wait-on http://127.0.0.1:8080
      - name: Run non-critical e2e tests
        env:
          CYPRESS_BASE_URL: http://127.0.0.1:8080
        run: npm run test:e2e:non-critical

  deploy:
    needs: [lint, unit-tests, e2e-tests-critical]
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      - name: Install Dependencies
        run: npm install
      - name: Build SASS
        run: npm run build
      - name: Setup Pages
        uses: actions/configure-pages@v2
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v1
